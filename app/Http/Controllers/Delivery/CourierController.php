<?php

namespace App\Http\Controllers\Delivery;

use Illuminate\Http\Request;

class CourierController extends Controller
{
    //myDeliveries() (Show only orders assigned to the logged-in courier), markDelivered() (Update status to "Delivered" and confirm cash collection).
}
